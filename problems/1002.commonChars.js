const input = ['bella', 'label', 'roller'];

const commonChars = function (A) {
  console.log(orgObject);

  A.map((value, index) => {

  });
  // var person = new Object();
//   A.map( (value,index)=>{
//       co
//   })
};

const ans = commonChars(input);
console.log(ans);
